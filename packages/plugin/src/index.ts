/**
 * @extendai/plugin — Plugin System
 *
 * Hook engine, plugin loader, capability manifest,
 * sandbox, hot-reload, and public SDK types.
 *
 * This is the heart of ExtendAI Lab — everything else
 * is just a plugin.
 */

export {}
