/**
 * @extendai/tui — Terminal UI
 *
 * React/Ink-based multi-pane terminal interface.
 * Session list, message flow, tool output, command palette.
 * Fully extensible via plugin system.
 */

export {}
